{
  "name": "wcp",
  "version": "1.0.0",
  "description": "Water Cooler Protocol — occupancy leases for many coding agents on one local checkout",
  "type": "module",
  "bin": {
    "wcp": "./src/cli.ts"
  },
  "scripts": {
    "wcp": "bun src/cli.ts",
    "test": "bun test",
    "build": "bun build --compile src/cli.ts --outfile dist/wcp"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "license": "MIT"
}
